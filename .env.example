# Optional. When set, the app uses the Claude API to personalize meal wording
# and food choices. Leave unset to run fully offline with deterministic templates.
#
# NOTE: In a production mobile app the key must live behind your own backend,
# never bundled into the client. This EXPO_PUBLIC_ var is for local demo only.
EXPO_PUBLIC_ANTHROPIC_API_KEY=
EXPO_PUBLIC_ANTHROPIC_MODEL=claude-haiku-4-5-20251001
